<section class="container mt-3 mt-lg-4">
    <div class="row">
        <div class="col text-center">
            <div class="card bg-accent text-adaptive border-adaptive py-4">
                <div class="card-header">
                    <h1 class="card-title">Landing page</h1>
                </div>
                <div class="card-body">
                    <div class="row d-flex justify-content-center">
                        <!-- Sign up href -->
                        <div class="col-6">
                            <a class="btn btn-lg text-adaptive border-adaptive me-2" href="@Url.Action("RegisterAcountPage", "Home")">Sign up</a> <a class="btn btn-lg text-adaptive border-adaptive" href="@Url.Action("LoginPage", "Home")">Log in</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
